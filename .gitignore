# Python bytecode cache
__pycache__/
*.py[cod]

# Claude Code session state (not part of the plugin)
.claude/

# Loom desktop app runtime state (mcp.json, temp reports) — never part of the repo.
.loom/
**/.loom/
